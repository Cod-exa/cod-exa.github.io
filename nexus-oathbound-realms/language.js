(() => { const lang = new URLSearchParams(location.search).get('lang') === 'en' ? 'en' : 'tr'; document.documentElement.lang = lang; document.querySelectorAll('[data-language]').forEach(el => { el.hidden = el.dataset.language !== lang; }); const h = document.querySelector(`[data-language="${lang}"] h1`); document.title = 'Nexus: Oathbound Realms — ' + (h ? h.innerHTML.replace(/<br\s*\/?\s*>/gi, ' ').replace(/<[^>]*>/g, '').trim() : 'CodExa'); document.querySelectorAll('.languages a').forEach(a => { if (a.lang === lang) a.setAttribute('aria-current', 'true'); }); })();
